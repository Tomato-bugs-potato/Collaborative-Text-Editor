apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: document-service
  template:
    metadata:
      labels:
        app: document-service
    spec:
      initContainers:
        # Wait for PostgreSQL master to be ready
        - name: wait-for-postgres
          image: postgres:15
          command: ['sh', '-c', 
            'until pg_isready -h postgres-master -U editor -d texteditor_docs; do echo "Waiting for postgres..."; sleep 2; done;
             echo "PostgreSQL is ready!"']
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
        # Run Prisma migrations
        - name: run-migrations
          image: document-service:latest
          imagePullPolicy: IfNotPresent
          command: ['sh', '-c', 'npx prisma db push --skip-generate']
          envFrom:
            - configMapRef:
                name: common-config
            - secretRef:
                name: app-secrets
          env:
            - name: DATABASE_URL
              value: "postgresql://editor:$(DB_PASSWORD)@postgres-master:5432/texteditor_docs"
      containers:
        - name: document-service
          image: document-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3002
          envFrom:
            - configMapRef:
                name: common-config
            - secretRef:
                name: app-secrets
          env:
            - name: PORT
              value: "3002"
            - name: INSTANCE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: DATABASE_URL
              value: "postgresql://editor:$(DB_PASSWORD)@$(DB_HOST):5432/texteditor_docs"
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: default
spec:
  ports:
    - port: 3002
      targetPort: 3002
  selector:
    app: document-service
